# Set the name explicitly; the shield default did not reach the compiled config.
CONFIG_ZMK_KEYBOARD_NAME="vibeboard"

# Test Bluetooth with the internal low-frequency oscillator in case the
# SuperMini's external 32.768 kHz crystal is faulty. Increases power usage.
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y
